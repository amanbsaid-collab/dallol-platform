[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["/worker/**", "/prisma/**", "/package.json", "/package-lock.json"]

[deploy]
startCommand = "node --import tsx src/index.ts"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
